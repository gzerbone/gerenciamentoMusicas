<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Álbum</title>
</head>

<body>

    <h1>Editar Álbum</h1>
    <% for(var i=0; i < dados.length; i++) { %>

        <form action="/editar-album/<%= dados[i].id_album %>" method="post" id="albumForm" class="container mt-5">            

                <label for="titulo_album">Título do Álbum:</label>
                <input type="text" name="titulo_album" id="album" value="<%= dados[i].titulo_album %>" required>
                <br>
                <label for="ano_lancamento">Ano de Lançamento:</label>
                <input type="text" name="ano_lancamento" id="ano_lancamento" value="<%= dados[i].ano_lancamento %>" required>
                <br>
                <label for="artista">Selecione um artista:</label>
                <select class="form-control" id="artista" name="id_artista">
                   
                    <% for(var i=0; i < dados.length; i++) { %>
                        <option value="<%= dados[i].id_artista %>">
                            <%= dados[i].nome %>
                        </option>
                        <% } %> <% } %>
                </select>
                <button type="submit">Salvar Alterações</button>
        </form>

</body>

</html>
